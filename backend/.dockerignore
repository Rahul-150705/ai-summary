# Compiled output — rebuilt inside Docker, no need to copy
target/

# Git history — not needed in the image
.git/
.gitignore

# IDE metadata
.idea/
*.iml
.vscode/

# OS junk
.DS_Store
Thumbs.db

# Docker files themselves
Dockerfile
docker-compose.yml
.dockerignore

# Env files (never include secrets in images!)
.env
*.env

# Docs
*.md
README*
